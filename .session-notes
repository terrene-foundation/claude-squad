## Session Notes [claude-squad] — updated 2026-04-08

### Read These Files First (in order)

1. `journal/0016-DISCOVERY-cc-credential-reload-mechanism.md` — CC source analysis proving mtime-based credential reload exists. The 0014 "cached forever" hypothesis is revised: CC DOES detect `.credentials.json` writes before every API call.
2. `journal/0014-GAP-swap-does-not-force-cc-reload.md` — original gap report. Context still valid but conclusions partially superseded by 0016.
3. `rotation-engine.py` — `swap_to()` ~line 816 (now has post-swap diagnostics ~line 947), `live_credentials_account()` ~line 486, `update_quota()` ~line 1037, `backsync()` ~line 1262.
4. `~/repos/contrib/claude-code-source-code/src/utils/auth.ts` — CC credential loading. Key: `getClaudeAIOAuthTokens` (1255, memoized), `invalidateOAuthCacheIfDiskChanged` (1320, mtime check), `checkAndRefreshOAuthTokenIfNeeded` (1427, called per API request).
5. `~/repos/contrib/claude-code-source-code/src/services/api/client.ts` — line 132 calls refresh check; line 303 bakes fresh token into new Anthropic client per turn.

### Accomplished

- **CC source code analysis**: Full credential flow traced. CC has mtime-based invalidation (auth.ts:1320), per-turn client construction (withRetry.ts:185), triple-check before refresh. Swap SHOULD work per code.
- **Verified live**: keychain format, service name, refresh token stability all match. Refresh tokens NOT rotated by Anthropic. Auto-rotate is disabled (no-op hook).
- **Swap diagnostics added**: `swap_to()` does immediate readback + 2s delayed check. Prints `DIAG(+2s): .credentials.json OVERWRITTEN` if CC races.
- **Committed**: `68a855b` (CO artifact cleanup) + `21d5ec5` (engine fixes + diagnostics). NOT pushed.
- **User's specific scenario** (swap after 3h idle → CC stays on old account): no code-level explanation found. Diagnostic added to catch it next time.

### Outstanding

- [ ] **Wait for swap diagnostic data** — next time swap fails, `DIAG(+2s)` output reveals the race. User's scenario: swap after hours idle.
- [ ] **Push commits** — 2 commits on main, not yet pushed. Branch protection requires PR + admin merge.
- [ ] **Update CLAUDE.md:26** — "in-place rotation works" is MORE valid now (CC has mtime detection), but user's failures are unexplained.
- [ ] **Add `.coc-sync-ignore`** — prevent loom/ sync from re-adding 17 deleted Kailash rules.
- [ ] **Optional cleanup**: `.claude/skills/` (3.6 MB), `.claude/agents/` (348 KB), `scripts/hooks/validate-workflow.js` (1060 lines).
- [ ] Pre-existing pyright warnings `rotation-engine.py:125/:140` (`_lock_file`/`_unlock_file` redecl) — harmless.

### Oversight — Verify Before Starting

- [ ] `git log --oneline -3` — should show `21d5ec5` and `68a855b`
- [ ] `bash test-platform.sh` — must show `PASS: 20  FAIL: 0`
- [ ] `diff -q rotation-engine.py ~/.claude/accounts/rotation-engine.py` — in sync
- [ ] `python3 -m py_compile rotation-engine.py` — must pass
- [ ] `ls .claude/rules/ | wc -l` — should be `13`

### Oversight — Verify After Completing

- [ ] `bash test-platform.sh` still 20/20 PASS
- [ ] `live_credentials_account()` used by BOTH `update_quota()` and `statusline_str()`
- [ ] Swap diagnostic (readback + delayed verify) still in `swap_to()`
- [ ] No new `os.replace` (use `_atomic_replace`) or `os.kill(pid, 0)` (use `_is_pid_alive`)

### Blockers

- **Diagnostic data needed**: swap-overwrite diagnostic must fire in the wild to close 0014. User runs 8 concurrent sessions — next stuck swap will produce the trace.
